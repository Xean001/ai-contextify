# Example project

This folder is just here to demonstrate `ai-contextify`.
Drop any mix of docs, code, and data here and run:

```bash
ai-contextify ./example
```

You'll get a `context-output/` folder you can paste straight into Claude / GPT / Gemini.
